# サーバー側ソース 完全一式（リデザイン版）

このフォルダの中身で、既存リポジトリの中身を置き換えれば動きます（下の「廃止」ファイルは削除）。

## 変更を適用済み／新規のファイル
| ファイル | 状態 | 説明 |
|---|---|---|
| main.py | ✅ 変更適用済み | migrate呼出・ルーター登録・/ をフロント配信・拡張配布削除 を適用済み。そのまま差し替え可 |
| database.py | ⚪ 変更なし（同梱） | 既存と同一。追加分は db_redesign.py に分離しているため無改造 |
| plans.py | 🆕 新規 | プラン定義（1monthのみ）・月間上限・レート秒数 |
| db_redesign.py | 🆕 新規 | DBマイグレーション（subscription_id/provider列・usage表）＋関連関数 |
| rate_limit.py | 🆕 新規 | 2分制限＋月間上限（コスト防御） |
| evaluate.py | 🆕 新規 | サーバー側Gemini採点 / POST /evaluate |
| payments.py | 🆕 新規 | Polar Webhook（＋2社目を足せるアダプタ層）/ POST /webhook/{provider} |
| frontend/index.html | 🆕 新規 | フロント（ログイン→貼付→採点→結果→CSV/Excel出力） |
| requirements.txt | ✅ 更新 | 既存＋polar-sdk |
| README_統合手順.md | 参考 | 詳細手順（読まなくても差し替えだけでOK） |
| requirements_additions.txt | 参考 | 追加分の差分メモ |

## 廃止（GitHubから削除してよい）
background.js / content_script.js / popup.html / popup.js / manifest.json（Chrome拡張一式）

## デプロイ手順（最短）
1. 上記ファイルをリポジトリに配置（main.py と database.py と requirements.txt は上書き、
   新規ファイルは追加、frontend/index.html も追加）
2. Chrome拡張5ファイルを削除
3. Render の環境変数に以下を追加:
   GEMINI_API_KEY / POLAR_WEBHOOK_SECRET / POLAR_PRODUCT_1MONTH
   （既存の ADMIN_USER / ADMIN_PASSWORD / BASE_URL はそのまま）
4. git push → 自動デプロイ。起動時に migrate() がDBを自動更新
5. トップページ（/）を開くとフロントが表示される
